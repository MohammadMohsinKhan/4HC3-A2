import React from 'react';

import BackButton from './BackButton';

const Notifications = () => (
  <div>
    <BackButton />
    <h1>Notifications</h1>
    <p>This is where all notifications are displayed.</p>
  </div>
);

export default Notifications;
